To create dir  ->  mkdir dir_name
To getin to that dir ->  cd dir_name
To create file ->  touch file_name.ext
To add that file -> git add file_name.ext
For status -> git status
For commit-> git commit -m "name for commit"
For adding remote repo to local repo -> git remote add origin "paste ssh"
To pull -> git pull origin master
To push->git push origin master 
To clone -> git clone
To add multiple files->git add  -A
For multiple commit-> git add -a -m "msge"
-----------------------------------------------
for branching merging and rebase
for branching -> git branch <branch name>
for switching from master to separate branch -> git checkout <branch name>
for merging -> git merge <branch name>
fir rebase -> git rebase master
              git rebase <branch name>
              



Thank you 
